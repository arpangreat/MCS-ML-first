[project]
name = "multiple-linear-regression"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "arpangreat", email = "arpanthegreat41@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.11.1",
    "numpy>=2.5.3",
    "pandas>=3.0.5",
    "scikit-learn>=1.9.0",
]

[project.scripts]
multiple-linear-regression = "multiple_linear_regression:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
