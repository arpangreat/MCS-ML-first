[project]
name = "kfold-cross-validation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "arpangreat", email = "arpanthegreat41@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
kfold-cross-validation = "kfold_cross_validation:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
